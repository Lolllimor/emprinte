# Landing migrations (source of truth)

Author SQL here for `landing.*` and shared workshop/bootcamp web tables.

Deploy via **mono root**: [`docs/MONOREPO.md`](../../../docs/MONOREPO.md) — `npm run db:compose` then `npm run db:push`.
